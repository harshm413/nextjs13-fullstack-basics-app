export function Student({ id, name, age, grade }) {
    return (
        <div>
            <p>ID: {id}</p>
            <p>Name: {name}</p>
            <p>Age: {age}</p>
            <p>Grade: {grade}</p>
            <hr />
        </div>
    );
}
